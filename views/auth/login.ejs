<%- include('../partials/header') %>

<section class="auth-page">
  <div class="container">
    <div class="auth-card">
      <div class="auth-header">
        <h1>🏪 Welcome to <span class="gold">Zaishah</span> Store</h1>
        <p>Sign in to create or manage your store</p>
      </div>

      <div class="auth-body">
        <% if (googleClientId && googleClientId !== 'GOOGLE_CLIENT_ID') { %>
          <!-- Google Sign In Button -->
          <div id="google-signin-btn" style="display: flex; justify-content: center; margin: 2rem 0;">
            <div id="g_id_onload"
              data-client_id="<%= googleClientId %>"
              data-callback="handleGoogleLogin"
              data-auto_prompt="false">
            </div>
            <div class="g_id_signin"
              data-type="standard"
              data-size="large"
              data-theme="outline"
              data-text="sign_in_with"
              data-shape="rectangular"
              data-logo_alignment="left"
              data-width="300">
            </div>
          </div>
          <script src="https://accounts.google.com/gsi/client" async defer></script>
          <script>
          function handleGoogleLogin(response) {
            fetch('/auth/google/callback', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ credential: response.credential })
            })
            .then(r => r.json())
            .then(data => {
              if (data.success) {
                window.location.href = data.redirect || '/seller/setup';
              } else {
                alert('Login failed. Please try again.');
              }
            })
            .catch(() => alert('Login failed. Please try again.'));
          }
          </script>
        <% } else { %>
          <div style="text-align: center; padding: 2rem 0;">
            <div style="font-size: 3rem; margin-bottom: 1rem;">🚧</div>
            <h3 style="margin-bottom: 0.5rem;">Coming Soon!</h3>
            <p style="color: var(--text-light); margin-bottom: 1.5rem;">Google Sign-In is being configured. Check back shortly!</p>
          </div>
        <% } %>

        <div class="auth-features">
          <div class="auth-feature">
            <span>✅</span> One-time PKR 5,000 registration
          </div>
          <div class="auth-feature">
            <span>✅</span> Your own branded storefront
          </div>
          <div class="auth-feature">
            <span>✅</span> Unlimited products
          </div>
          <div class="auth-feature">
            <span>✅</span> Accept card payments
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include('../partials/footer') %>
